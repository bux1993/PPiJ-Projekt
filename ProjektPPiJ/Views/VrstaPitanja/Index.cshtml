@model IEnumerable<ProjektPPiJ.Models.VrstaPitanja>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VrstaPitanja1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VrstaPitanja1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VrstaPitanjaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VrstaPitanjaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VrstaPitanjaID })
        </td>
    </tr>
}

</table>
